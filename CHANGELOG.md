# Changelog

## 2026-06-08

- 重新创建自用英语复习打卡 App 项目文档目录。
- 明确语音输入同时保留手机系统输入法语音和 App 内单独语音按钮。
- 明确每天超过 15 条的到期内容保持逾期状态，不跳过。
- 补充“忘了 / 模糊 / 熟了”影响复习节奏的建议规则，等待最终确认。
- 补充 `JSON` 导出和导入的含义说明，并保持第一版优先只做导出备份。
- 确认第一版不做导入备份，只做一键导出 `JSON` 备份。
- 确认 App 内语音按钮允许麦克风授权，失败时退回系统输入法语音。
- 确认掌握反馈排期：忘了 1 天后，模糊 3 天后，熟了按正常下一档。
- 新增 `README.md`，说明项目目标、第一版范围、复习规则、备份规则和后续启动方式。
- 创建最小 Web App 骨架，支持本地启动。
- 实现第一阶段功能：新增内容、本地保存、内容库基础查看。
- 改为零依赖本地启动方式，避免第一版因为安装依赖卡住。
- 修复零依赖启动后的 Chrome 白屏问题：改为在 `index.html` 中标准引入 CSS。
- 新增 `experiments/gemini-preview.html`，用于单独预览 Gemini 生成的 HTML 方案，不替换当前 App。
- 参考 Gemini 预览版的版面方向，重做正式版为手机优先航海手账界面。
- 新增底部导航：复习、新增、内容库、备份。
- 新增正式版每日复习、答案遮挡、掌握反馈和打卡记录。
- 新增 App 内语音按钮，浏览器不支持时提示使用系统输入法语音。
- 新增正式版一键导出 `JSON` 备份，不加入导入和危险清空按钮。
- 调小正式版整体字号、边框和阴影，降低视觉压迫感。
- 优化 App 内语音输入提示，显示更具体的权限、麦克风和未检测到声音原因。
- 将顶部 Logo 结构改为可替换图片的容器，后续可固定用户提供的头像。
- 将顶部 Logo 替换为用户提供的本地头像 `assets/nami-avatar.jpg`。
- 补充手机端 App 内语音仍需浏览器或系统麦克风授权的说明。
- 将新增页两个听写按钮图标从闪电改为电话虫风格图标。
- 新增 `docs/media/pwa-vs-native-demo.html`，用动画方式解释本地开发、在线 PWA 和原生封装的区别。
- 新增 PWA 基础配置：`manifest.webmanifest`、`service-worker.js`、`apple-touch-icon` 和 `.nojekyll`。
- 将正式版资源路径改为相对路径，兼容 GitHub Pages 仓库子路径部署。
- 将线上部署仓库切换为 `kina222/english-review-checkin-app`，并确认 GitHub Pages 地址可公开访问。
- 调整新增页输入框字号到 `16px`，减少手机浏览器聚焦时自动放大。
- 优化 App 内听写体验，支持显示临时识别文字并区分英文、中文追加间隔。
- 更新静态资源版本号和离线缓存名，减少手机端继续读取旧版本的问题。
- 内容库新增编辑和删除能力，支持修改英文/中文字段，并用软删除避免误删后备份中完全丢失。
- 更新静态资源版本号和离线缓存名，确保手机端能加载内容库编辑/删除功能。
- 调整 App 内听写临时识别文字显示位置：直接显示在当前英文或中文输入框中。
- 补充本地存储保护规则，明确不能随意更换主数据 key，避免版本更新造成已录入内容丢失。
- 新增标准 PNG 桌面图标，确保添加到主屏幕时使用娜美头像。
- 新增当前版本显示和“刷新到最新版”按钮，用于清理旧 PWA 缓存且不删除学习数据。
- 优化 service worker：页面导航优先联网获取新版，离线时再回退缓存。
- 新增 `JSON` 合并导入：重复内容自动跳过，新内容补进当前本地数据，便于换浏览器、换手机或换桌面入口后恢复。
- 更新静态资源版本号和离线缓存名，确保手机端能加载导入备份功能。
- 调整新内容复习节奏：录入后第 2 天第一次复习，熟了后 3 天后再复习，再进入第 7 天、第 15 天、第 30 天和 30 天循环。
- 更新 2026-06-17 手动导入文件，将 16 条内容的首次复习日期改为 2026-06-18。
- 新增 `.gitignore`，避免本地临时导入文件被误提交到公开仓库。
- 内容库和复习卡片新增英文朗读按钮，使用浏览器/系统自带语音朗读英文，帮助复习发音。
- 更新静态资源版本号和离线缓存名，确保手机端能加载朗读功能。
- 将底部导航改为固定在屏幕底部显示，避免内容库列表较长时需要滚到最底部才能切换页面。
- 增加内容区底部留白，避免最后一条内容被固定导航遮挡。
- 调整复习流程：先根据提示输入自己想起来的答案，再点击“对照答案”查看标准答案和选择“忘了 / 模糊 / 熟了”。
- 更新静态资源版本号和离线缓存名；本次不修改本地存储 key，避免影响已保存内容和打卡记录。
- 手动新增内容时增加重复拦截：内容库已有同样英文和中文时不再重复添加，并提示“内容库里已经有这条了”。
- 更新静态资源版本号和离线缓存名，确保手机端加载新增去重逻辑。
- 新增轻量音效：添加成功、重复提示、对照答案、熟了、模糊、忘了会播放不同的轻提示音。
- 复习时新增英文发音提示，使用本地常见词音标词表显示近似 IPA，不依赖外部网络。
- 更新静态资源版本号和离线缓存名，确保手机端加载音效和发音提示。

## 2026-06-06

- 初始化自用英语复习打卡 App 项目文档。
- 明确第一版 MVP 范围：新增内容、内容库、自动复习计划、每日复习、答案遮挡、掌握反馈、每日打卡、每日总结、本地存储、一键导出备份、手机桌面入口。
- 明确第一版不做：标签、来源、备注、登录、云同步、AI 纠错、朗读、OCR、社交、排行榜。
- 新增需求、页面流程、数据模型、架构和排障文档入口。
